# NGODS 数据平台 Airflow 环境变量配置

# Airflow 用户 ID（Linux/Mac 用户运行 `id -u` 获取）
AIRFLOW_UID=50000

# Airflow Docker 镜像
AIRFLOW_IMAGE_NAME=apache/airflow:3.0.3

# 可选：额外的 Python 包
# _PIP_ADDITIONAL_REQUIREMENTS=your-additional-packages 